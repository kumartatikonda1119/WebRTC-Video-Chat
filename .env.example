# App server port
PORT=3000

# STUN server exposed to the browser for ICE discovery
NEXT_PUBLIC_STUN_SERVER=stun:stun.l.google.com:19302

# Optional override for Socket.IO CORS origin in production
SOCKET_IO_CORS_ORIGIN=*
